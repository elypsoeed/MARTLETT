type: object
required:
  - name
properties:
  name:
    type: string
    nullable: false
    minLength: 1
    maxLength: 255
    pattern: "^[A-Za-z0-9._-]+$"
  visibility:
    $ref: "../RepositoryVisibility.yaml"
